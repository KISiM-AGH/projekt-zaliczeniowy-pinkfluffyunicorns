import {Container} from "@mantine/core";

export const MainPage = () => {
    return (
        <Container size="xs" padding="xs">
            Tu będzie nasz sklep jeśli będzie skończony backend
            </Container>
    )
}